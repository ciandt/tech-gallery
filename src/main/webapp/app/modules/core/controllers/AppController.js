module.exports = function ($rootScope, AppService) {

  /**
   * Object context
   * @type {Object}
   */
  var context = this;

  /**
   * User login state
   * @type {Boolean}
   */
  this.isUserLogged = true;
}
